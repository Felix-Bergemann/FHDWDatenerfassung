{% extends 'base.html.twig' %}
{% block body %}


    <section>
        <div class="m-4">
                    <h3>Messverlauf {{clientname}}</h3>
        </div>
        <div class="d-flex justify-content-around mb-3 p-3">
           <div class="d-inline">
                <div class="col-lg-3 mb-4">
                    {% for chart in charts %}
                        {{ mukadi_chart(chart) }}
                    {% endfor %}
                </div>
            </div>       
        </div>
    </section>
{% endblock %}



